version: "0.2"

env:
  shell: bash

phases:
  install:
    commands:
      - ./scripts/ci-setup.sh
  build:
    commands:
      - earthly --use-inline-cache +archive

artifacts:
  files:
    - /dist/*
  discard-paths: "yes"

cache:
  paths:
    - $HOME/.cargo/registry/**/*
