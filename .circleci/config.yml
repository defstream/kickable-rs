version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run: sudo ./scripts/ci-setup.sh
      - run: earthly --ci +archive

  build_normal:
    docker:
      - image: cimg/rust:1.65.0
    parallelism: 4
    steps:
      - checkout
      - run: sudo ./scripts/ci-setup.sh
      - run: make build